'use strict';

//DOM получение эллементов со страницы

/*const box = document.getElementById(`box`); 
console.log(box);

const btns = document.getElementsByTagName(`button`);
console.log(btns[3]);

const circles = document.getElementsByClassName(`circle`);
console.log(circles);

const hearts = document.querySelectorAll('.heart');
hearts.forEach(item => {
    console.log(item);
});

const wrapper = document.querySelector(`.wrapper`);
const oneHeart = wrapper.querySelector(`.heart`);
console.log(oneHeart);

box.style.cssText = `background-color: red; width: 300px; height: 200px`;

btns[1].style.borderRadius = `100%`;

/*for (let i = 0; i , hearts.length; i++) {
    hearts[i].style.backgroundColor = `blue`;
}*/

/*hearts.forEach (item => {
    item.style.backgroundColor = `blue`;
});

const div = document.createElement(`div`);
//const text = document.createTextNode(`Тут был я!`);

div.classList.add(`black`);

document.body.append(div); //div добавляется в самый конец body
//wrapper.appendChild(div);

//wrapper.prepend(div); //div добавляется в начале родителя

//hearts[1].before(div); // before говорит перед каким элементом нужно вставить div

//circles[1].after(div); //after говорит после какого эллемента вставиться div

//wrapper.insertBefore(div, hearts[0]);

//circles[0].remove(); // удаляет элемент
//wrapper.removeChild(hearts[1]);

circles[0].style.backgroundColor = `red`;

//hearts[0].replaceWith(circles[0]);
//wrapper.replaceChild(circles[0],hearts[0]);


div.innerHTML = `<h1>Hello World!</h1>`;

//div.textContent = `Hello`;

div.insertAdjacentHTML(`afterend`,`<h2>Bla</h2>`); // позволяет вставлть HTML эллемент рядом с указываемым элементом

const body = document.querySelector(`body`);

body.style.background = "bg.jpg";*/

// Events